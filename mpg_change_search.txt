'change' found at index 40674: ...             <label class="conference-option">
                    <input type="checkbox" checked onchange="toggleConference('${conf.replace(/'/g, "\\'")}')">
                    ${conf}
                </label>
            `).join('');

            updateConferenceButton();
            filterGroup....

'delta' found at index 11021: ...        text-align: center;
            padding: 40px;
            color: #666;
        }

        .delta-neg {
            color: #f87171;
        }

        .delta-pos {
            color: #4ade80;
        }

        .download-btn {
            background: #1a1a1a;
            border: 1px solid #3...

'prevWeek' not found

'previous' not found

'lastWeek' not found

'diff' not found