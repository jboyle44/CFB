name: Check Real Remaining
on:
  workflow_dispatch: {}
jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install requests
      - working-directory: scripts
        env:
          CFBD_API_KEY: ${{ secrets.CFBD_API_KEY }}
        run: python3 check_real_remaining.py
      - run: |
          git config user.name "test-bot"
          git config user.email "actions@github.com"
          git add real_remaining.txt
          git commit -m "Real remaining check" || echo "nothing to commit"
          git push
