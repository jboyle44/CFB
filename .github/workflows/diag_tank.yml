name: Diagnostic Tankathon 2
on:
  workflow_dispatch: {}
jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install requests beautifulsoup4
      - working-directory: scripts
        run: python3 diag_tankathon.py
      - run: |
          git config user.name "test-bot"
          git config user.email "actions@github.com"
          git add tankathon_diag2.txt
          git commit -m "Diagnostic 2" || echo "nothing to commit"
          git push
